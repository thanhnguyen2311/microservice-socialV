server:
  port: 8080
spring:
  application:
    name: "user-service"
  profiles:
    active: "dev"
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8071/}"

management:
  endpoints:
    web:
      exposure:
        include: "*"
